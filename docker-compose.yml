services:
  libre-search:
    image: ${DOCKERHUB_IMAGE:-libre-search:latest}
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${APP_PORT:-8080}:80"
    volumes:
      - libre_search_data:/var/www/html/data
    restart: unless-stopped

volumes:
  libre_search_data:
