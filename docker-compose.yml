services:
  libre-search:
    image: ${DOCKERHUB_IMAGE:-liberchat/libre-search:latest}
    ports:
      - "${APP_PORT:-8080}:80"
    volumes:
      - libre_search_data:/var/www/html/data
    restart: unless-stopped

volumes:
  libre_search_data:
